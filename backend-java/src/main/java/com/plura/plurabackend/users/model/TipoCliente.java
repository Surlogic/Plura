package com.plura.plurabackend.users.model;

public enum TipoCliente {
    // Tiene un local físico.
    LOCAL,
    // Profesional independiente (categoría específica).
    PROF,
    // No tiene local; trabaja a domicilio.
    SIN_LOCAL
}
