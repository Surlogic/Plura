{
  "name": "plura",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently \"pnpm -C backend dev\" \"pnpm -C apps/web dev\"",
    "dev:web": "pnpm -C apps/web dev",
    "dev:backend": "pnpm -C backend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
